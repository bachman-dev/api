{
  "name": "@bachman-dev/api-server",
  "private": true,
  "version": "0.1.0",
  "description": "",
  "type": "module",
  "scripts": {
    "build": "pnpm check-types && wrangler deploy --dry-run --outdir dist",
    "check-types": "tsc --noEmit",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "wrangler d1 migrations apply api-bachman-dev",
    "deploy": "pnpm check-types && wrangler deploy --minify",
    "dev": "wrangler dev",
    "dev:cron": "wrangler dev --test-scheduled",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "start": "node ./dist/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@bachman-dev/api-types": "workspace:*",
    "@hono/zod-validator": "0.4.1",
    "drizzle-orm": "0.34.1",
    "hono": "4.6.5",
    "zod": "3.23.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "4.20241018.0",
    "drizzle-kit": "0.25.0",
    "typescript": "5.6.3",
    "wrangler": "3.80.4"
  }
}
